{
  "name": "@cadence/service",
  "private": true,
  "version": "0.1.0",
  "dependencies": {
    "@cadence/claude-agent-sdk-rs": "workspace:*"
  },
  "scripts": {
    "build": "cargo build",
    "build:release": "cargo build --release",
    "dev": "cargo build",
    "test": "cargo test",
    "lint": "cargo check"
  }
}
