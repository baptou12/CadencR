{
  "name": "@cadence/service",
  "private": true,
  "version": "0.1.0",
  "dependencies": {
    "@cadence/claude-agent-sdk-rs": "workspace:*"
  },
  "scripts": {
    "build": "cargo build",
    "build:release": "cargo build --release",
    "dev": "cargo watch -w src -x 'run -- --db-path ~/Library/Application\\ Support/cadence/cadence.db --port 5005'",
    "test": "cargo test",
    "lint": "cargo check"
  }
}
