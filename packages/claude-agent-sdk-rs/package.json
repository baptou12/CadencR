{
  "name": "@cadence/claude-agent-sdk-rs",
  "private": true,
  "version": "0.1.0",
  "description": "Rust SDK wrapping the Claude Code CLI — used by Cadence to stream and control Claude sessions.",
  "license": "Apache-2.0",
  "scripts": {
    "build": "cargo build",
    "build:release": "cargo build --release",
    "dev": "cargo build",
    "test": "cargo test",
    "lint": "cargo check",
    "format": "cargo fmt",
    "format:check": "cargo fmt -- --check"
  }
}
