{
  "name": "@cadence/claude-agent-sdk-rs",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "cargo build",
    "build:release": "cargo build --release",
    "test": "cargo test",
    "lint": "cargo check"
  }
}
