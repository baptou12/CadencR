---
interface Props {
  lead: string;
}
const { lead } = Astro.props;
---
<div class="rounded-xl border border-dashed border-border px-6 py-12 text-center text-[13px] text-muted-foreground">
  <span class="text-foreground">{lead}</span>
  <slot />
</div>
