---
interface Props {
  class?: string;
}
const { class: klass } = Astro.props;
---
<p
  class={`text-[13.5px] leading-[1.6] text-muted-foreground max-w-[560px] [text-wrap:pretty] [&_.em]:text-foreground ${klass ?? ""}`}
>
  <slot />
</p>
