---
interface Props {
  class?: string;
}
const { class: klass } = Astro.props;
---
<p
  class={`text-[15px] leading-[1.65] text-muted-foreground max-w-[54ch] [text-wrap:pretty] [&_.em]:text-foreground [&_.em]:font-medium ${klass ?? ""}`}
>
  <slot />
</p>
