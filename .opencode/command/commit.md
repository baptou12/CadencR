---
description: Write any missing tests, verify changes, and create a commit
---
Run the repository's commit workflow.

Load and follow `.claude/skills/test-commit/SKILL.md` for the full workflow, including:

- checking current-session changes only
- writing any missing tests first
- running the relevant test commands
- creating the git commit without `git stash` and without `--no-verify`

Additional commit context:
$ARGUMENTS
